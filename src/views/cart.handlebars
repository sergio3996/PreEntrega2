<table>
  <tr>
    <th>Producto</th>
    <th>Precio</th>
    <th>Stock</th>
    <th></th>
  </tr>
  <tbody>
    {{#each products}}
      <tr>
        <td>{{this.title}}</td>
        <td>{{this.price}}</td>
        <td>{{this.stock}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>